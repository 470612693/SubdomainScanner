name: Manual dispatch test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print info
        run: |
          echo "Manual dispatch works"
          echo "branch: ${{ github.ref }}"
